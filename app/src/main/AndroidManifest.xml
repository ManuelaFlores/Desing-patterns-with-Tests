<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="pe.tohure.desingpatternswithtests">

    <application
        android:name="pe.tohure.designpatternswithtests.app.DesignPatternApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="pe.tohure.designpatternswithtests.ui.customeraccount.CustomerAccountActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="pe.tohure.designpatternswithtests.ui.customeraccountdetail.CustomerAccountDetailActivity"></activity>
    </application>

</manifest>